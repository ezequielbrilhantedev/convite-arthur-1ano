# URL do Web App do Google Apps Script que grava as confirmações na planilha.
# Publique o script em apps-script/Code.gs (ver README) e cole a URL /exec aqui.
# Se ficar vazio, o formulário continua funcionando, mas NÃO grava as presenças.
VITE_RSVP_ENDPOINT=
